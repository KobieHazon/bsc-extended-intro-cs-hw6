[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bsc-extended-intro-cs-hw6"
version = "1.0.0"
description = "Combinatorics, rolling hashes, compression, image matrices, and repetition-code decoding"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Kobie Hazon" }]
dependencies = []

[project.scripts]
extended-intro-hw6 = "extended_intro_hw6_cli:cli"

[dependency-groups]
dev = [
    "pytest>=8.0,<9",
    "ruff>=0.12,<1",
]

[tool.hatch.build.targets.wheel]
only-include = ["src/extended_intro_hw6.py", "src/extended_intro_hw6_cli.py"]
sources = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = ["assignment", "solution", ".venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
